# MEASUREMENTS.md — Background Check API 실측 결과

- 대상: `https://54capvm12g.execute-api.ap-northeast-2.amazonaws.com` (첨부 swagger.yaml 의 Production Server)
- 측정 도구: `measure/bg_measure.py` (Python 표준 라이브러리만 사용, 판단 없이 수치만 기록)
- 본 실행: run-id `HBRC-FULL-0903`, 2026-09-03 01:54 ~ 02:28 UTC, 총 785 요청, 클라이언트 타임아웃 60s, 타임아웃 발생 0건
- 원자료: `measure/results/HBRC-FULL-0903/requests.jsonl` (요청 1건 = 1줄), `lifecycle.jsonl`, `summary.md`
- 예비 실행: `HBRC-TEST`(129 요청, 타임아웃 30s), `HBRC-TEST2`(24 요청). 본문 수치는 별도 표기가 없으면 본 실행 기준.
- employeeId 는 실행마다 고유 접두어(`HBRC-FULL-0903-…`)를 붙여 다른 사용자 데이터와 분리했다. (목록 조회에서 8월 23일자 타인 데이터가 섞여 나온 것을 예비 실행에서 확인)

---

## 1. GET /background-checks/{checkId} 응답 지연

순차 호출, 요청 간격 0.2s, checkId 풀 21개 순환. **표본 n = 200.**

| 구분 | n | min | p50 | p90 | p95 | p99 | max |
|---|---|---|---|---|---|---|---|
| 전체 | 200 | 53 ms | 238 ms | 30,045 ms | 30,121 ms | 31,113 ms | 32,045 ms |
| HTTP 200 만 | 67 | 96 ms | 701 ms | 26,231 ms | 27,846 ms | 32,045 ms | 32,045 ms |
| HTTP 500 만 | 74 | 53 ms | 78 ms | 260 ms | 1,029 ms | 5,181 ms | 5,181 ms |
| HTTP 503 만 | 59 | 56 ms | 320 ms | 30,191 ms | 30,930 ms | 31,538 ms | 31,538 ms |
| 전반부 100건 | 100 | 53 ms | 149 ms | 30,045 ms | 30,063 ms | 31,538 ms | 31,538 ms |
| 후반부 100건 | 100 | 56 ms | 340 ms | 29,922 ms | 30,292 ms | 32,045 ms | 32,045 ms |

관측:
- 지연 분포는 두 덩어리다. 수백 ms 이내에 끝나는 응답과 30초 부근(30.0~32.0s)에 끝나는 응답. 그 사이 구간은 거의 없다.
- 30초 부근 응답은 200 과 503 양쪽 모두에서 나온다. 즉 "오래 걸린 뒤 성공"과 "오래 걸린 뒤 503"이 모두 존재한다.
- 500 은 대부분 100 ms 이내에 즉시 돌아온다 (p90 260 ms).
- 전반부/후반부 분포가 같아 시간 경과나 호출 누적에 따른 변화는 관측되지 않았다.

## 2. HTTP 상태코드 분포

같은 200건 기준.

| HTTP | 건수 | 비율 |
|---|---|---|
| 200 | 67 | 33.5% |
| 500 | 74 | 37.0% |
| 503 | 59 | 29.5% |

응답 본문 형태:

| 본문 | 건수 |
|---|---|
| `{"error":"Internal Server Error","message":"Service temporarily unavailable. Please try again later.","statusCode":500}` | 74 |
| `{"error":"Service Unavailable","message":"The service is currently overloaded. Please retry after the specified time.","retryAfter":30,"statusCode":503}` | 37 |
| `{"message":"Service Unavailable"}` (retryAfter 없음, API Gateway 기본 형태) | 22 |

- `Retry-After` 응답 헤더는 785건 전체에서 0건. 본문 `retryAfter` 는 503 의 일부(37/59)에만 있고 값은 항상 30.
- 같은 checkId 에 대한 GET 이 200 → 500 → 503 → 200 처럼 상태코드가 요청마다 바뀐다. 상태코드는 checkId 나 호출 시점이 아니라 호출 건별 확률로 결정되는 것으로 보인다.

전체 785건(모든 단계 합산) 상태코드: 200 137, 201 100, 400 10, 404 12, 204 1, 500 292, 503 233.

## 3. 같은 employeeId 로 POST 반복

`HBRC-FULL-0903-DUP-001` 로 0.5s 간격 POST. **표본 n = 10** (+ 다른 이름·생년월일로 1회).

| 항목 | 결과 |
|---|---|
| 상태코드 | 10건 모두 201 |
| 응답 checkId | 10개 모두 서로 다름 (고유 10/10) |
| 초기 status | pending 4, flagged 6 |
| POST 지연 | p50 78 ms, p95 154 ms, max 154 ms |
| 같은 employeeId + 다른 firstName/lastName/dateOfBirth 로 POST | 201, 새 checkId |
| 그 뒤 `GET /background-checks?employeeId=` 의 totalCount | 11 |

관측: 중복 검사·멱등 처리·거부(409 등)는 없다. POST 는 호출할 때마다 새 조회 건을 만든다. 같은 사번으로 이름이 달라도 그대로 생성된다. (반복 전후 목록 조회 2회는 500 으로 실패해 totalCount 를 못 받았고, 다른 이름 POST 뒤 조회에서 11 을 확인)

## 4. pending 에서 최종 상태까지 걸리는 시간

시드 10명으로 10건 생성 후 2s 간격 폴링, 상한 180s. **표본 n = 10 (초기 pending 5건).**

| 항목 | 결과 |
|---|---|
| POST 즉시 최종 | 5/10 (clear 4, flagged 1) |
| POST 시 pending | 5/10 → 모두 최종 도달 (clear 4, flagged 1), 타임아웃 0 |
| `estimatedCompletionSeconds` | 10건 모두 응답에 없음 |
| 폴링 중 HTTP | 200 10, 500 7, 503 6 (총 23회) |
| 체크당 폴링 횟수 | 1, 3, 5, 3, 1, 4, 1, 1, 3, 1 |

소요 시간 (초):

| 기준 | n | min | p50 | p95 | max | mean |
|---|---|---|---|---|---|---|
| 클라이언트 관측, 초기 pending 5건 | 5 | 83.6 | 127.9 | 168.1 | 168.1 | 127.0 |
| 서버 `createdAt`→`completedAt`, 최종 도달 10건 (즉시 완료 5건은 0) | 10 | 0.0 | 78.1 | 153.2 | 153.2 | 53.5 |

예비 실행(`HBRC-TEST`, n=4, pending 2건)에서는 17.5s, 33.1s 로 완료되었다. 본 실행에서는 84~168s. 같은 API 인데 실행에 따라 완료 시간이 5배 이상 다르다.

최종 응답 값 분포(n=10): criminalRecord false 9 / true 1, educationVerified true 8 / false 2, employmentVerified true 8 / false 2, creditScore good 4 / fair 3 / poor 3. 최종 응답 본문은 10건 모두 명세의 12개 필드를 그대로 갖고 있었다.

## 5. 동시 요청 수를 늘렸을 때

레벨별 50건, GET 은 완료된 checkId 풀 순환, POST 는 새 employeeId. 레벨 사이 5s 휴지.

| 동시 수 | 종류 | n | wall | p50 | p95 | max | 200/201 | 500 | 503 | 타임아웃 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | GET | 50 | 342.9 s | 253 ms | 30,053 ms | 30,115 ms | 19 | 19 | 12 | 0 |
| 5 | GET | 50 | 87.4 s | 489 ms | 30,102 ms | 30,189 ms | 25 | 14 | 11 | 0 |
| 10 | GET | 50 | 31.8 s | 89 ms | 30,040 ms | 30,068 ms | 16 | 18 | 16 | 0 |
| 20 | GET | 50 | 31.1 s | 521 ms | 30,081 ms | 30,349 ms | 17 | 16 | 17 | 0 |
| 50 | GET | 50 | 30.2 s | 271 ms | 30,070 ms | 30,085 ms | 21 | 15 | 14 | 0 |
| 1 | POST | 50 | 4.3 s | 81 ms | 103 ms | 123 ms | 50 | 0 | 0 | 0 |
| 5 | POST | 50 | 1.1 s | 95 ms | 133 ms | 531 ms | 50 | 0 | 0 | 0 |
| 10 | POST | 50 | 1.0 s | 119 ms | 457 ms | 465 ms | 50 | 0 | 0 | 0 |
| 20 | POST | 50 | 1.6 s | 128 ms | 1,401 ms | 1,469 ms | 50 | 0 | 0 | 0 |
| 50 | POST | 50 | 1.4 s | 133 ms | 1,260 ms | 1,277 ms | 50 | 0 | 0 | 0 |

관측:
- GET 은 동시 수를 1→50 으로 올려도 상태코드 비율(약 1/3 씩)과 p95(약 30s)가 변하지 않는다. 동시성 증가로 인한 추가 실패·지연은 없다.
- POST 는 250건 전부 201. 동시 20 이상에서 p95 가 1.3~1.4s 로 커지지만 실패는 없다.
- 예비 실행(타임아웃 30s)에서는 동시 5·10 에서 타임아웃 2건씩 발생했다. 본 실행(타임아웃 60s)에서는 그 요청들이 30~32s 에 503 또는 200 으로 돌아왔다. 즉 30s 타임아웃은 정상 응답 일부를 잘라낸다.

## 6. 명세(swagger.yaml) 대조

프로브 38건 (`summary.md` 5절 전체 표).

### 6-1. 명세에 적혀 있으나 실제로는 확인되지 않은 것

| 명세 | 실제 |
|---|---|
| POST 201 응답의 `estimatedCompletionSeconds` ("폴링 주기 결정에 쓰라") | 110건의 201 응답 어디에도 없음 |
| 503 의 `Retry-After` 헤더 ("고정 백오프 대신 이 값을 따르라") | 785건 중 0건. 본문 `retryAfter` 만 일부(37/233)에 있고 값은 항상 30 |
| 존재하지 않는 checkId 는 404 | 형식이 맞는 미존재 checkId 는 500(본 실행) 또는 503(예비 실행). 404 는 형식이 깨진 값(`not-a-check-id`)에서만 |
| 목록 조회 `employeeId` 누락 시 400 | 본 실행 400, 예비 실행에서는 500·타임아웃. 일관되지 않음 |
| 500 응답 본문 `{error, message, statusCode}` | 그 형태와 `{"message":"Internal Server Error"}` 두 가지가 섞임 |

### 6-2. 명세에 없으나 실제 호출에서 발견한 동작

- `dateOfBirth` 형식 검증 없음: `1990/03/15`, `2099-01-01`(미래), `1990-02-30`(없는 날), `abc`, `1990-03-15T00:00:00Z` 모두 201.
- `Content-Type: text/plain` 으로 보낸 JSON 도 201.
- `employeeId` 를 숫자(12345)로 보내면 400 이 아니라 500.
- 500자 employeeId 도 201.
- 같은 employeeId 로 POST 를 반복하면 매번 새 checkId (3절).
- 목록 조회는 다른 사용자가 만든 데이터도 같은 employeeId 면 함께 반환 (공유 저장소).
- 정상 checkId GET 이 500/503 을 돌려주는 비율이 2/3 (2절). 명세는 500/503 을 "가능한 응답"으로만 적었고 빈도는 없다.
- 한글 firstName/lastName 도 그대로 201. 로마자 강제 없음.
- 미정의 메서드(PUT/DELETE/PATCH/HEAD)는 404 `{"message":"Not Found"}`, OPTIONS 는 204.

### 6-3. 명세에 적힌 값이 실제 상황에 맞지 않는다고 판단한 것

- `estimatedCompletionSeconds` 예시 20초: 실제 pending→완료는 예비 실행 17~33s, 본 실행 84~168s. 값 자체가 오지 않을 뿐 아니라 예시 20초와도 맞지 않는다.
- `retryAfter: 30` / `Retry-After: 30`: 503 뒤 30초를 기다릴 근거가 관측되지 않았다. 503 직후 재요청해도 성공률은 같은 1/3 이고, 반대로 503 자체가 30초 걸려서 오는 경우가 절반이다.
- 명세 설명 "If pending, poll until the status changes": 폴링 한 번의 성공률이 1/3 이라 "상태가 바뀔 때까지" 만으로는 종료 조건이 되지 않는다.

---

## 7. 실측값으로 정한 값 (backend `application.yml` `bgcheck.*`)

| 항목 | 값 | 근거 |
|---|---|---|
| 연결 타임아웃 | 3 s | 연결 실패는 785건 중 0건. 연결 단계 지연은 관측되지 않았으므로 짧게 잡아도 잃는 응답이 없다. |
| 읽기 타임아웃 | 35 s | 응답 지연 p99 31.1s, max 32.0s (n=200). 30s 로 자르면 예비 실행에서 실제로 정상 200 응답을 타임아웃으로 버렸다(6건/40). 35s 는 max 를 3s 여유로 넘기면서 그 이상 기다려도 응답이 없다는 것(32s 초과 0건)에 맞춘 값. |
| POST 재시도 횟수 | 3회 | POST 는 300건 전부 201(2xx 실패 0건). 재시도는 명세상 가능한 5xx 를 위한 보험이고, 실측 실패율 0 이므로 3회면 충분하다. 4xx 는 재시도하지 않는다(입력 오류). |
| POST 재시도 간격 | 1 s | 5xx 가 관측되지 않아 간격의 근거를 실측에서 얻지 못했다. POST p95 가 1.4s 이내이므로 그 수준으로 두었다. `Retry-After` 는 오지 않으므로 헤더 기반 대기는 구현하지 않았다. |
| 폴링 주기 | 5 s | 폴링 1회 성공률 1/3, 1회 지연은 절반이 30s. 주기를 30s 이상으로 늘려도 성공률은 오르지 않고(2절, 상태코드는 호출별 확률), 짧게 하면 30s 짜리 응답이 겹친다. 스케줄러는 fixedDelay 라 한 바퀴 끝난 뒤 5s 를 세므로 겹치지 않고, 빠른 응답(수백 ms)일 때는 5s 마다 재시도한다. |
| 폴링 중단: 경과 시간 | 240 s | 서버 기준 pending→완료 p95 153s, max 153s (n=10, pending 5). 그 1.5배 수준. 예비 실행의 33s 도 포함. |
| 폴링 중단: 횟수 | 40회 | 240s 동안 5s 주기면 최대 48회이지만 30s 짜리 응답이 섞이면 훨씬 적다. 40회는 응답이 모두 빠를 때의 상한이고, 경과 시간과 둘 중 먼저 닿는 쪽에서 멈춘다. |
| 404 | 즉시 FAILED | 형식이 깨진 checkId 에서만 404 가 나왔다. 재시도해도 바뀌지 않는다. |
| 500/503 | 폴링 계속 | 같은 checkId 가 다음 호출에 200 을 주는 것을 반복 관측(2절). 5xx 는 "이 건이 실패했다"가 아니라 "이번 호출이 실패했다"는 뜻이다. |
| 상한 초과 후 | TIMEOUT 상태로 두고 관리자가 GET 만 재확인 | checkId 는 유효하고 서버 쪽 처리는 계속되므로(4절: 168s 뒤 완료) 새 POST 를 내면 이력만 늘어난다. |

동시성: 동시 50 까지 상태코드 비율과 지연이 변하지 않았으므로 앱에서 외부 호출 동시 수를 제한하지 않았다. 폴링은 PENDING 건을 순차로 GET 하며, 한 건당 최대 35s 이므로 PENDING 이 N 건이면 한 바퀴가 최대 35N 초다. 이 앱의 사용량(관리자 1명, 직원 10명)에서는 문제되지 않는다.
